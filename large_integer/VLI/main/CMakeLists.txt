if(VLI_COMPILE_GPU)
    include_directories(${CUDA_INCLUDE_DIRS})
    add_definitions(-DVLI_USE_GPU -DVLI_SPLIT_PARAM=${VLI_SPLIT_PARAM} )
endif(VLI_COMPILE_GPU)
add_executable (a.out main.cpp )
target_link_libraries (a.out  ${PROJECT_BINARY_DIR}/src/vli/libvli_cpu.a) 
if(VLI_COMPILE_GPU)
    target_link_libraries (a.out ${GMP_LIBRARY_CXX} ${OPENMP_LIBRARIES} ${PROJECT_BINARY_DIR}/src/vli/libvli_gpu.a ${CUDA_LIBRARIES})
else(VLI_COMPILE_GPU)
    target_link_libraries (a.out ${GMP_LIBRARY_CXX} ${OPENMP_LIBRARIES} ${OpenMP_CXX_FLAGS})
    #target_link_libraries (a.out ${OPENMP_LIBRARIES})
    #target_link_libraries (a.out)
endif(VLI_COMPILE_GPU)
